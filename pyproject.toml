[project]
name = "ospd-team4-fall2025"
version = "0.1.0"
description = "Team 4 – Operating Systems Programming & Design (HW2)"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "fastapi", 
    "uvicorn",
    "slack_api @ file:///opt/render/project/src/src/slack_api",
    "slack_impl @ file:///opt/render/project/src/src/slack_impl",
     "slack_service @ file:///opt/render/project/src/src/slack_service"

]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.ruff]
target-version = "py312"
line-length = 100
