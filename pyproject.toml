[build-system]
requires = ['setuptools>=61.0']
build-backend = 'setuptools.build_meta'

[project]
name = 'mail-client-workspace'
version = '0.1.0'
requires-python = '>=3.12'
dependencies = []

[tool.setuptools.packages.find]
where = ['src']
include = ['*']
exclude = []
[project.optional-dependencies]
dev = []
[project.optional-dependencies]
dev = ["ruff", "mypy", "pytest", "pytest-cov"]
